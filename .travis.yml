dist: xenial
sudo: required
language: node_js
node_js:
  - "6"
before_install:
  - sudo chmod +x install-chroot.sh
  - sudo ./install-chroot.sh
install:
  - sudo echo "done!"
script:
  - sudo echo "finally test!"
